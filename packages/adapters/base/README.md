# sentinel-adapter-base

Shared **plugin protocol**, metadata, and discovery helpers for Sentinel framework adapters.

```python
from sentinel_adapter_base import AdapterMetadata, AdapterProtocol
```

Does not depend on any agent framework.
